#include "Assembler.h"

int main() {
//    tbmrk mark_table = {};
//    mark_table.table = (mrk*)calloc(ASM_KEK_MARK_BUFF_SIZE, sizeof(mrk));
//    mark_table.capacity = ASM_KEK_MARK_BUFF_SIZE;

    assembler();
//    disassembler("disassembler.txt");
    return 0;
}
